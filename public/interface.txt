const BLACK = 0;
const WHITE = 1;

function necklace(n, c[]) callbacks {
    procedure reverse_interval(name_first_bead, name_last_bead);
}

main {
    read n;
    for i to n {
        read c[i];
    }

    call min_mosse = necklace(n, c);

    write min_mosse;
}
